_format_version: '3.0'
_transform: true

services:
  - name: api-gateway
    url: http://api-gateway:3000
    routes:
      - name: api-gateway
        paths:
          - /
        strip_path: false

